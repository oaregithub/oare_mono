{
  "name": "oare",
  "private": true,
  "scripts": {
    "start": "yarn workspace @oare/backend start",
    "lint": "yarn workspaces run lint",
    "lint:fix": "yarn workspace @oare/frontend lint:fix",
    "build": "./build.sh",
    "bootstrap": "lerna bootstrap --use-workspaces",
    "serve": "yarn workspace @oare/frontend serve",
    "test": "yarn workspace @oare/frontend test"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "husky": "^4.3.0",
    "lerna": "^3.22.1",
    "lint-staged": "^10.4.0"
  },
  "husky": {
    "hooks": {
      "pre-push": "yarn test"
    }
  }
}
