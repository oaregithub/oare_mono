{
  "name": "oare",
  "private": true,
  "scripts": {
    "start": "cd packages/backend && yarn start",
    "build": "./build.sh"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
